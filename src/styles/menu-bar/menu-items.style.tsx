import styled from "styled-components";

const MenuItemsHeader = styled.header`
  display: flex;
  justify-content: flex-end;
  align-items: center;
`;

const MenuItemsOptions = styled.ul`
  display: flex;
  justify-content: flex-end;
  list-style-type: none;
`;

export { MenuItemsHeader, MenuItemsOptions };
